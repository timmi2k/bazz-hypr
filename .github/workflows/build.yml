name: bluebuild
on:
  schedule:
    # 06:20 UTC - Bazzite baut seine Images kurz vorher, so bekommen wir jeden
    # Tag die aktuelle Base mit.
    - cron: "20 06 * * *"
  push:
    paths-ignore:
      - "**.md"
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  bluebuild:
    name: Build bazz-hypr
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        recipe:
          - recipe.yml
    steps:
      - name: Build Custom Image
        uses: blue-build/github-action@v1.11
        with:
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ${{ github.event.number }}
          # bazzite-nvidia-open ist gross, dazu kommen Qt6, das vorgebaute
          # Python-venv und zwei git-Checkouts. Ohne das Aufraeumen des
          # Runners geht dem Build der Plattenplatz aus.
          maximize_build_space: true
